// eslint-disable-next-line no-unused-vars
declare namespace Express {
  export interface Request {
    username: string
  }
}

/**
 * Isso permite modificar a estruta de Request do express e adicionar uma propriedade personalizada
 */
